import type { NextPage } from 'next';

const Resources: NextPage = () => {
  return (
    <div>
      <h2 className="text-3xl font-bold text-center mb-6">Tài nguyên</h2>
      <p className="text-center text-gray-600">Đây là trang Tài nguyên.</p>
    </div>
  );
};

export default Resources;